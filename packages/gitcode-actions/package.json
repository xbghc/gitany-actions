{
  "name": "@xbghc/gitcode-actions",
  "version": "0.1.0",
  "description": "GitCode Actions - Automated workflows and GitHub Actions for GitCode platform",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -b",
    "clean": "rm -rf dist",
    "lint": "eslint .",
    "cleanup": "node ./scripts/cleanup.mjs",
    "test": "pnpm --filter @gitany/shared build && pnpm --filter @xbghc/gitcode-api build && tsx tests/watch-issues-detect-new-comments.test.ts"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "dependencies": {
    "@xbghc/gitcode-cli": "workspace:*",
    "@xbghc/gitcode-api": "workspace:*",
    "dockerode": "^4.0.8",
    "tar": "^7.4.3",
    "pino": "^9.0.0",
    "pino-pretty": "^11.0.0"
  },
  "devDependencies": {
    "@types/dockerode": "^3.3.43"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
