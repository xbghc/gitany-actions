{
  "name": "@gitany/shared",
  "version": "0.1.0",
  "description": "Shared utilities for the GitAny monorepo (logger, etc).",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "dev": "pnpm build && node dist/index.js",
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "lint": "eslint ."
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "devDependencies": {},
  "dependencies": {
    "pino": "^9.0.0"
  },
  "engines": {
    "node": ">=18.17"
  }
}
