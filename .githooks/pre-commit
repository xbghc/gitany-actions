#!/usr/bin/env bash
set -euo pipefail

# Only run if we have a git repo and node available
if ! command -v node >/dev/null 2>&1; then
  echo "[docs-check] node is not available, skipping." >&2
  exit 0
fi

node scripts/check-docs-updated.mjs

