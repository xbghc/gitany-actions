# 脚本环境变量示例
# 将此文件复制为 scripts/.env 并填写真实值

# --- 全局设置 ---

# GitAny / GitCode 鉴权
GITANY_TOKEN=
GITCODE_TOKEN=
GITCODE_API_BASE=https://gitcode.com/api/v5
GITCODE_AUTH_STYLE=bearer
GITCODE_AUTH_HEADER=

# Claude / Anthropic (chat 功能需要)
ANTHROPIC_API_KEY=
ANTHROPIC_API_BASE=
ANTHROPIC_PROXY=

# HTTP 调试与日志
GITCODE_HTTP_DEBUG=
GITCODE_HTTP_DEBUG_SHOW_SECRETS=
GITANY_LOG_LEVEL=

# --- watch-ai-mentions.mjs 专用设置 ---
#
# 监听仓库中的 @ 用户或自定义 token, 并调用 chat 进行回复。

# 目标仓库 URL (必填)
REPO_URL=

# 自定义触发标记 (可选, 默认 @ 当前用户名)
MENTION_TOKEN=

# 轮询间隔 (可选, 单位: 秒)
ISSUE_INTERVAL_SEC=
PR_INTERVAL_SEC=

# 监听持续时长 (可选, 单位: 秒, 到时后自动退出)
WATCH_DURATION_SEC=

# 要监听的评论类型 (可选, 默认都监听)
INCLUDE_ISSUE_COMMENTS=true
INCLUDE_PR_COMMENTS=true

# chat 相关设置
RUN_CHAT=true # 设置为 false 则为 dry-run, 不实际调用 chat
CHAT_SHA= # 指定 chat 使用的 SHA (可选)
CHAT_NODE_VERSION= # 指定 chat 容器的 Node.js 版本 (可选)
CHAT_KEEP_CONTAINER=false # 设置为 true 则保留 chat 容器 (可选)
CHAT_VERBOSE=false # 设置为 true 则输出 chat 详细日志 (可选)

# 调试选项
VERBOSE=false # 设置为 true 则打印评论正文等调试信息
SHOW_PROMPT=false # 设置为 true 则输出生成的提示词

# --- 其他测试脚本设置 ---

# test-claude-chat.mjs
TEST_QUESTION=

# test-container-comment.mjs
TEST_PR_NUMBER=
TEST_COMMENT_BODY=
TEST_CONTAINER_IMAGE=
TEST_PR_MAX_PAGES=

# 通用测试设置 (可能被其他旧测试脚本使用)
TEST_REPO_URL=
TEST_SHA=
TEST_NODE_VERSION=
TEST_KEEP_CONTAINER=
TEST_VERBOSE=

# 包管理器镜像 (容器内部使用)
NPM_CONFIG_REGISTRY=
PNPM_CONFIG_REGISTRY=
